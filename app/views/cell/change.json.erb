<% broadcast "/table/edit" do %>
  <% unless @cells.empty? %>
  <%= raw({
    "data-time" => @updated_at.to_i,
    "data" => changes_json(@cells, @cell_index, @history_time_i)
  }.to_json) %>
  <% end %>
<% end %>
